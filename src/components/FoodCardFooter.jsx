import { Button, Card } from "react-bootstrap";

export const FoodCardFooter = () => {
    return (
        <Card.Footer>
            <Button variant="primary">Order</Button>
        </Card.Footer>
    );
}

export default FoodCardFooter;